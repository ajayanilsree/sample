document.addEventListener('change',function(event){if(!event.target.matches('.role-picker input[type="radio"]'))return;const fields=document.querySelector('.login-fields');if(fields){fields.hidden=false;requestAnimationFrame(function(){fields.classList.add('revealed');});const username=fields.querySelector('input[name="username"]');if(username) username.focus();}});document.querySelectorAll('.show-password').forEach(function(button){button.addEventListener('click',function(){const input=button.parentElement.querySelector('input');const visible=input.type==='text';input.type=visible?'password':'text';button.textContent=visible?'Show':'Hide';button.setAttribute('aria-label',visible?'Show password':'Hide password');});});
